# Backend (FastAPI) çalışma zamanı bağımlılıkları
# Gerekçeler: .docs/shared/decisions/0001-tech-stack.md + README

fastapi>=0.110          # web framework + otomatik OpenAPI/Swagger
uvicorn[standard]>=0.29 # ASGI sunucu
pydantic>=2.6           # I/O + validasyon modelleri (validasyonun kalbi)
pydantic-settings>=2.2  # .env'den tip-güvenli ayar okuma
python-multipart>=0.0.9 # CSV dosya yükleme (multipart)
pandas>=2.2             # CSV parse + normalize + agregasyon
SQLAlchemy>=2.0         # SQLite ORM
httpx>=0.27             # hedef API HTTP client
tenacity>=8.2           # retry + exponential backoff
python-dotenv>=1.0      # .env yükleme (yedek)
openpyxl>=3.1           # indirilebilir validation report (Excel — bonus)
